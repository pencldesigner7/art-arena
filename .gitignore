# secrets — never commit (server/.env.example IS the template, keep it)
.env
*.env
!*.env.example

# runtime / user data
node_modules/
dev-outbox.json
server/avatars/
*.log

# local DB dumps (first-boot schema bootstrap is server/schema.sql, committed)
latest.sql
*.sql.dump
